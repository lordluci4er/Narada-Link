import 'package:flutter/material.dart';

class AppColors {
  static const background = Colors.black;

  static const primary = Colors.white;
  static const secondary = Colors.white70;

  static const card = Color(0xFF121212);
  static const input = Color(0xFF1E1E1E);

  // 🔥 NEW (message bubbles + UI contrast)
  static const myMessage = Color(0xFF2C2C2C);     // dark gray
  static const otherMessage = Color(0xFFE0E0E0);  // light gray

  static const divider = Color(0xFF2A2A2A);
}